using OnAppearing.Models;
using System.Collections.ObjectModel;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace OnAppearing.ViewModels
{
    public class SearchViewModel
    {   

        



        /*
        
        #region INotifyPropertyChanged implementation

        public event PropertyChangedEventHandler PropertyChanged;

        protected virtual void RaiseOnPropertyChanged([CallerMemberName] string propertyName = null)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }

        #endregion
        
        private ObservableCollection<Ordner> _ordner = new ObservableCollection<Ordner>();
        public ObservableCollection<Ordner> Ordners
        {
            get => _ordner;
            set
            {
                _ordner = value;
                RaiseOnPropertyChanged();
            }
        }

        public SearchViewModel()
        {
            for (var i = 0; i < 5; i++)
            {
                Ordners.Add(new Ordner
                {
                    Name = $"Ordner {i}"
                });
            }
        }

        */
    }

}

